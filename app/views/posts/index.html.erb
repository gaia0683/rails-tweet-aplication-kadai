<h1>ポスト一覧</h1>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @posts.each do |post|%>
  <tr>
    <td><%= post.content%></td>
    
    <td><%= link_to "ブログを編集する", edit_post_path(post.id) %></td>
  </tr>
<% end %>
</table>

<br>

<%=link_to "新しいポストを投稿する", new_post_path %>